module.exports = {
    name: 'token',
    version: '1.0.0',
    register: (request, option) => {
        console.log('request', request)
        console.log('option', option)


        request.on('request', )
    }   
}